# Write code to take a csv file ,
# which contains sensor data in columns ,
# build a ETL pipeline to extract column values ,
# find central tendencies of column  values and
# store all these values in a new csv file ,
# use pyspark only , where pipeline runs parallely
